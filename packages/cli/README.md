# @define/cli

CLI for scaffolding and generating Define artifacts.

## Install

```bash
pnpm add -D @define/cli
```

## Commands

- `define init`
- `define generate`
- `define generate:db`
- `define generate:repos`
- `define generate:openapi`
