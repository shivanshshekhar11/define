# @define/adapter-fastify

Fastify adapter utilities for Define.

## Install

```bash
pnpm add @define/adapter-fastify
```

## Provides

- Request validation from resource metadata
- JSON Schema generation
- Response serializers
- Reusable CRUD methods (`createCRUDMethods`)
- Optional route registration wrapper (`registerCRUDRoutes`)
